language: javascript
branches:
  only:
    - master
before_install:
  - nvm install && nvm use && nvm version
  - node -v
  - npm -v
install:
  - npm install
script:
  - npm run lint
  - npm run test-ci
after_script:
  - npm run update-coverage
